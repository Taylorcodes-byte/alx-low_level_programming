#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int m;
	srand(time(0));
	m = rand() - RAND_MAX / 2;
	if (n > 0)
	{
		printf"%d is positive\n", m);
	}
	else if (n == 0)
	{
		printf("%d is zero\n", m);
	}
	else
	{
		printf("%d is negative\n", m);
	}
	return (0);
}
